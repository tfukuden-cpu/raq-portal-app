# Raq Works 機能説明書

> 作成日: 2026-06-09  
> 対象バージョン: 現行本番  
> 対象読者: スタッフ・管理者・運営者

---

## 目次

1. [システム概要](#1-システム概要)
2. [ログイン・初期設定](#2-ログイン初期設定)
3. [スタッフメニュー](#3-スタッフメニュー)
4. [管理者メニュー](#4-管理者メニュー)
5. [運営者メニュー](#5-運営者メニュー)
6. [打刻端末（タブレット）](#6-打刻端末タブレット)
7. [共通仕様・ルール](#7-共通仕様ルール)

---

## 1. システム概要

Raq Works は合同会社Raq の社内ポータルアプリです。スタッフの勤怠管理・シフト管理・座席配置・社内連絡をひとつのアプリで行います。

### アクセス方法
- **URL**: https://raq-portal-app.vercel.app
- **動作環境**: スマートフォン・タブレット・PC のブラウザ（インストール不要）
- **推奨**: ホーム画面に追加するとアプリ感覚で使えます（PWA対応）

### ロール（権限）の種類

| ロール | できること |
|--------|----------|
| **スタッフ** | スタッフメニューのみ（シフト確認・出勤報告・問い合わせ等） |
| **案件管理者** | スタッフメニュー＋管理者メニュー（シフト管理・勤怠管理等） |
| **グローバル管理者** | 全案件の管理者メニュー |
| **運営者** | 全機能＋運営者メニュー（アカウント管理・案件作成等） |

### 案件（プロジェクト）について
- スタッフは1つまたは複数の「案件」に所属します
- 複数案件に所属している場合、画面右上またはマイページから案件を切り替えできます
- シフト・勤怠・座席などのデータはすべて案件ごとに管理されます

---

## 2. ログイン・初期設定

### ログイン方法
1. ブラウザで URL を開く
2. メールアドレスを入力（例: `s001@raq.internal`）
3. パスワードを入力
4. 「ログイン」をタップ

> メールアドレスは社員ID（S001 など）の小文字 + `@raq.internal` です。パスワードは管理者から初回配布されます。

### LINE連携（必須）
ログイン後、LINE連携が未設定の場合は自動的に連携ページへ移動します。
- LINEアプリを開いてログインし、許可を行うと連携完了
- 連携するとシフト変更・欠勤通知などがLINEで届くようになります
- LINE公式アカウントの「友達追加」も合わせて行ってください（通知受信に必要）

### 視点モード（管理者のみ）
案件管理者は「スタッフ視点」と「管理者視点」を切り替えられます。
- マイページ下部の「表示モード」で切り替え
- スタッフ視点にすると管理機能が非表示になり、スタッフと同じ画面で確認できます

---

## 3. スタッフメニュー

> 全スタッフが使えるメニューです

### 3-1. ホーム（`/dashboard`）

**概要**: ログイン直後に表示されるホーム画面です。

**表示内容**:
- スタッフ名と時間帯別のあいさつ
- 本日のシフト情報（シフト名・開始〜終了時刻）
- 勤怠ステータス（勤務中・休憩中・退勤済みなど）
- 未読のお知らせタイムライン
- 1週間カレンダー（今日をネイビー丸でハイライト）

**アクションボタン**:

| ボタン | 動作 | 条件 |
|--------|------|------|
| **出発報告** | 出勤前の出発を報告。管理者に LINE 通知が届く | 打刻前のみ表示 |
| **欠勤報告** | 当日欠勤を報告（理由選択）。管理者に LINE 通知が届く | 打刻前のみ表示 |
| **遅刻報告** | 遅刻を報告（到着予定時刻を選択）。管理者に LINE 通知が届く | 打刻前のみ表示 |
| **経過報告** | 欠勤報告済みの場合に表示。当日の状態を報告 | 欠勤報告後のみ表示 |

> 出勤打刻（clock_in）が記録された後は、欠勤・遅刻ボタンは表示されません。

---

### 3-2. シフト（`/shifts`）

**概要**: 自分のシフト表を月カレンダー形式で確認できます。

**機能**:
- 前後3ヶ月のシフトをカレンダーで表示
- 各日付に「シフト名」「開始〜終了時刻」が表示
- 希望休申請済みの日付にはバッジが表示（申請中=紫・承認済=濃紫・却下=赤）

**希望休申請（右上ボタン）**:
1. 「希望休申請」ボタンをタップ
2. カレンダーで申請したい日付をタップ
3. 優先度を選択（第一〜第四希望）
4. 申請完了

**希望休の優先度**:
| 優先度 | バッジ色 | 意味 |
|--------|---------|------|
| 第一希望 | 青 | 最も重要な休み |
| 第二希望 | 藍 | 次に重要な休み |
| 第三希望 | 紫 | できれば休みたい |
| 第四希望 | グレー | あれば嬉しい |
| 冠婚葬祭 | 赤 | 特別事由 |

**申請の制限**（案件ごとに管理者が設定）:
- 月の申請可能日数の上限
- 土日の申請可能日数の上限
- 申請期日（期日を過ぎると取り下げ不可）
- 1日あたりの申請人数の上限
- 連続申請の上限日数

---

### 3-3. 勤怠実績（`/record`）

**概要**: 自分の月次勤怠実績を確認できます。

**表示内容**:
- 月ナビゲーション（前月・翌月への移動）
- 各日付の出勤時刻・退勤時刻・休憩時間・稼働時間
- シフトとの差異（遅刻・早退など）

> 月切り替えは URL の `?month=YYYY-MM` で直接指定もできます

---

### 3-4. 投稿（`/post`）

**概要**: 案件内の全スタッフが見られる社内掲示板です。

**機能**:
- テキスト投稿（最新100件表示）
- 自分の投稿を削除

> 管理者は全員の投稿を削除できます（視点モード「スタッフ」時は削除ボタン非表示）

---

### 3-5. 周知事項（`/notices`）

**概要**: 管理者から配信されたお知らせを確認できます。

**機能**:
- お知らせ一覧（ピン留め=常に最上部、通常=新着順）
- タップして全文を展開
- 既読管理（確認済みは既読マーク）
- 未読数はホーム画面・ナビバーのバッジに表示

**お知らせの種類**:
- 全体宛: 案件内の全スタッフに配信
- 個人宛: 特定のスタッフのみに表示

---

### 3-6. 問い合わせ（`/inquiries`）

**概要**: 管理者に問い合わせを送信できます。

**送信フォーム**:

| 項目 | 内容 | 必須 |
|------|------|------|
| お問い合わせ種別 | シフト・勤務 / 休暇・申請 / 勤怠・打刻 / アカウント・ログイン / その他 | ○ |
| 件名 | 問い合わせの件名（短く要約） | ○ |
| 詳細内容 | 問い合わせの詳細（最大2000文字） | ○ |
| 添付ファイル | 画像・書類（合計10MBまで） | ✗ |
| 緊急度 | 高（早急）/ 中（通常）/ 低（余裕あり） | ○ |

**問い合わせ履歴の確認**:
- フォームの下（スマホ）または右サイドバー（PC）に「問い合わせ履歴 / 返信状況」が表示
- 各履歴をタップして全文・返信内容を確認
- ステータス: 受付中（黄）/ 解決済み（緑）
- 管理者から返信があると LINE で通知が届く（全文表示）

---

### 3-7. 勤怠補正申請（`/corrections`）

**概要**: 打刻漏れや誤りがあった場合に補正を申請できます。

**申請内容**:
- 対象日付
- 正しい出勤時刻・退勤時刻
- 申請理由

**ステータス**:
| ステータス | 説明 |
|-----------|------|
| 申請中 | 管理者が未審査 |
| 承認済み | 管理者が承認し打刻記録に反映済み |
| 却下 | 管理者が却下（理由コメント付き） |

> 審査結果は LINE で通知されます

---

### 3-8. 欠勤経過報告（`/absence-followup`）

**概要**: 当日欠勤のスタッフが夕方（17:00前後）に経過を報告するページです。  
LINE の「経過報告する」ボタンからアクセスします。

**報告フォーム**:

| 項目 | 選択肢 |
|------|--------|
| 症状等 | 発熱（体温入力あり）・頭痛・咳・倦怠感・吐き気・その他（自由記入） |
| 軽快状況 | 改善 / 横ばい / 悪化 |
| 当日受診状況 | 受診あり / 受診なし |
| 翌日出勤予定 | シフトDBから自動表示（変更不可） |
| 翌日出勤可否 | 出勤可能 / 出勤困難 |
| 次回出勤可否報告予定 | 出勤困難を選択した場合のみ入力 |

> 送信すると管理者・グループLINEに自動報告フォーマットで届きます  
> 送信済みの場合は完了画面が表示され再送不可です

---

### 3-9. マイページ（`/my`）

**概要**: 自分のプロフィールと各種設定を管理します。

**表示・設定内容**:

| 項目 | 内容 |
|------|------|
| プロフィール | 名前・社員ID |
| アバター | キャラクター選択（顔・髪・服などカスタマイズ） |
| LINE連携 | 連携状態の確認・解除。友達追加状況 |
| 通知設定 | ブラウザプッシュ通知のON/OFF |
| 視点モード | スタッフ表示 ↔ 管理者表示の切り替え（管理者のみ） |
| 案件切り替え | 複数案件所属時のみ表示 |
| ログアウト | セッションを終了 |

---

## 4. 管理者メニュー

> `project_admin` 以上のロールが使えるメニューです

### 4-1. 当日状況（`/attendance`）

**概要**: 当日の全スタッフの出勤状況をリアルタイムで管理します。4つのタブで構成されています。

**日付ナビゲーション**: 上部の「◀ ▶」で前日・翌日の状況も閲覧できます

---

#### タブ①: 出勤簿

全スタッフの当日状況を「セクション別ボード」で表示します。

**スタッフカードのステータス**:

| ステータス | 色 | 説明 |
|-----------|---|------|
| 勤務中 | 緑 | 出勤打刻済み・稼働中 |
| 休憩中 | 橙 | 休憩中（経過時間タイマー表示） |
| 離席中 | グレー | 座席を離れている（経過時間タイマー表示） |
| 退勤済 | 濃グレー | 退勤打刻済み ※ボードから非表示 |
| 未出勤 | 白 | まだ出勤打刻なし |
| 欠勤 | 赤 | 欠勤報告あり |
| 遅刻 | アンバー | 遅刻報告あり |

**セクションヘッダー（各セクションの上部）**:
- 配置数・必要枠数（充足 ±N）を表示
- 販売・査定セクションは早番/遅番の内訳も表示

**出力ボタン（右上）**:
- 当日の出勤簿をExcelで出力
- 早番シート・遅番シートの2シート構成
- シフト開始時刻が11:00前=早番、11:00以降=遅番で自動振り分け
- アカウント番号昇順で並び替え

**お休みスタッフパネル（ボード右端の横スクロール末尾）**:
- 公休・有休スタッフを表示
- 「打診」ボタン: そのスタッフに出勤依頼LINEを送信
- 「不可」ボタン: 打診不可マークをつける（赤バッジで表示）

**H MOTAスロット配置パネル**（H MOTAセクション内）:
- スタッフをドラッグ＆ドロップで12:00〜13:00/18:00〜19:00スロットに配置
- × ボタンで削除

---

#### タブ②: 確定後変更

シフト確定後に発生した変更を確認するビューです。

- セクション別・アカウント番号順で全スタッフを一覧表示
- 確定版（月初設定値）と当日版（実際の状況）を並列表示
- 差分のある行はアンバーでハイライト

---

#### タブ③: 座席表

座席レイアウト上に当日のスタッフ配置を表示します。

**座席カードの色**:
| 状態 | 色 |
|------|---|
| 勤務中 | 緑 |
| 休憩中 | 橙 |
| 超過（休憩オーバー） | 赤 |
| 離席中 | グレー |
| 退勤済み | 濃グレー |

**操作**:
- 座席タップ → 打刻モーダル（出勤・退勤・離席・休憩操作）
- 編集モード: スタッフを座席にドラッグ配置
- 「休憩一覧」ボタン: スロット別・セクション別・早遅番別の休憩人数・名前を一覧表示
- 「休憩割り振り」ボタン: 番付順に基づき自動でスロット割り当て

---

#### タブ④: 打刻記録

全出勤者の当日打刻を横棒ガントチャートで表示します。

**表示内容**:
- 各スタッフの出勤〜休憩〜離席〜退勤の時系列
- 査定・販売スタッフ行に「休憩スロット変更」ドロップダウン（①②③スロット）
- 小休憩分数セレクタ（なし/10/15/20/30分）

---

### 4-2. 勤怠管理（`/attendance/edit`）

**概要**: スタッフの勤怠記録を詳細に管理します。4つのタブで構成されています。

---

#### タブ①: 勤怠修正

スタッフから提出された打刻補正申請を審査します。

**一覧表示項目**:
- スタッフ名・対象日・申請した出勤時刻・退勤時刻・申請理由
- SV承認者名（残業・早退申請と照合）
- ステータス（審査中 / 承認済 / 却下）

**フィルター**: 審査中のみ / すべて / 承認済 / 却下

**承認操作**:
1. 該当行の「承認」ボタンをクリック
2. 確認モーダルで「承認」→ 打刻記録（punch_logs）が正しい時刻に上書きされる
3. スタッフに LINE で審査結果が通知される

**却下操作**:
1. 「却下」ボタンをクリック
2. 却下理由を入力して送信
3. スタッフに LINE で却下通知が届く

**再適用ボタン**（承認済みのみ）:
- タイムスタンプ不具合等で未反映だった申請を再度打刻記録に適用するためのボタン

---

#### タブ②: 申請一覧

早退・残業申請の一覧を確認します（現状は閲覧のみ）。

**表示項目**:
- スタッフ名・シフト日・種別（早退 / 残業）・SV署名・ステータス

**フィルター**: すべて / 早退 / 残業

---

#### タブ③: 勤怠実績

月次の勤怠データを詳細に確認・修正できます。

**操作フロー**:
1. 月ナビゲーション（◀ ▶）で対象月を選択
2. 名前検索フィールドでスタッフを絞り込み
3. スタッフ行をクリックして詳細カレンダービューへ移動

**詳細カレンダービュー（スタッフ個人）**:

各日付の行に以下を表示:

| 列 | 内容 |
|----|------|
| 操作 | 「修正」ボタン |
| 日付 | M/D（曜日）+ 状態バッジ（遅刻・早退） |
| シフト | シフト名（公休・希望休含む） |
| 出勤 | 打刻時刻（HH:MM） |
| 退勤 | 打刻時刻（HH:MM） |
| 休憩 | 休憩時間 |
| 稼働 | 実稼働時間 |
| 超過 | 超過時間（橙色） |
| 確定 | 確定ボタン（確定済み=緑「確定済」） |
| 備考 | 出勤打刻・退勤打刻の実打刻時刻、早退/残業承認者名、修正者名など |

**備考欄の記録内容**:
- `出勤打刻: 09:43` — 実際に打刻した時刻（記録時刻とは異なる場合あり）
- `退勤打刻: 18:15  早退承認者: 田中SV` — 退勤実打刻時刻と承認者
- `修正者: S001` — 管理者が直接修正した場合

**「修正」ボタン（管理者直接修正）**:
- 出勤・退勤時刻を直接変更できます
- 修正後は備考欄に `修正者: 管理者名` が記録されます

**「確定」ボタン**:
- その日の勤怠を確定状態にします
- 確定済み = 緑「確定済」ボタン（タップで確定解除）

**月移動時もスタッフ選択が維持**されます（URLにstaffIdが保持）。

**出力ボタン**: ExcelまたはPDF形式で出力（ExportModal）

**月計フッター**: 月の合計稼働時間・超過時間を集計表示

---

#### タブ④: 遵守率

シフト遵守率（予定通りに出勤した割合）を確認します。

---

### 4-3. シフト管理（`/shifts/manage`）

**概要**: 全スタッフのシフトを一括管理します。5つのタブで構成されています。

---

#### タブ①: シフト（メインビュー）

月次シフトカレンダーをスタッフ行 × 日付列のグリッドで表示します。

**上部ツールバー**:
| ボタン | 機能 |
|--------|------|
| ◀ YYYY/MM ▶ | 月ナビゲーション |
| シフト通知（LINE） | 展開済みシフトをLINEで各スタッフに送信 |
| 展開済み / 未展開 | 現在の展開状態を表示 |
| Excel出力 | シフト表をExcelファイルでダウンロード |
| シフト編集 | 編集モードに切り替え |

**充足テーブル**（グリッド上部）:
- セクション別・シフトパターン別の必要枠数と実数を日付ごとに表示
- セルの色: 充足=緑 / 不足=赤 / 超過=アンバー
- 日付ヘッダーをクリック → ソート・フィルターポップオーバーが開く

**日付ヘッダーのソート・フィルター**:
- クリックでポップオーバーを表示
- **ソート**: セクション順 / シフト順（開始時刻）
- **フィルター（複数選択可）**: セクション絞り込み / シフト名絞り込み
- フィルター有効時: 日付ヘッダーがアンバーハイライト（▼マーク）
- 「クリア」でリセット

**スタッフ行の公開/非公開トグル**:
- 各スタッフ名の横に「公開」/「非公」ボタン
- 「非公」に設定したスタッフは、シフト通知（LINE展開）の対象から除外される
- 設定値は `project_members.shift_published` に保存

---

#### タブ①（編集モード）: シフト編集グリッド

「シフト編集」ボタンを押すと全画面の編集グリッドに切り替わります。

**編集操作**:
- 各セルをクリック → シフトパターン選択ピッカーが表示
- セルをドラッグ → 範囲選択して一括設定
- 「公休」「希望休」「有休」「特別休暇」も選択可能

**スタッフ行の操作**:
- 公開/非公 トグルボタン（シフト展開の送信対象から除外）
- 離職リスクフラグの切り替え（赤い⚠マーク）

**日付列ヘッダーのソート・フィルター**:
（シフトビューと同じ操作）

**仮組み機能**:
- 「仮組み」ボタンで条件に合うシフトパターンを自動生成
- 生成したドラフトは保存ボタンで確定するまで仮の状態

**確定・通知**:
- 「保存」→ 変更差分をDBに保存
- 「変更を通知」→ 変更対象スタッフにLINE通知
- 「シフト展開」→ 公開設定のスタッフ全員に月次シフトをLINE送信

---

#### タブ②: シフト設定

シフトパターン（シフト名・セクション・開始/終了時刻・必要枠数）を登録・編集します。

---

#### タブ③: 希望休

スタッフからの希望休申請を確認・承認します。

---

#### タブ④: 研修設定

研修日程を管理します。

---

#### タブ⑤: 変更ログ

シフト変更の履歴（誰が・いつ・どのスタッフの・どの日を・どう変えたか）を確認します。

---

### 4-4. メンバー管理（`/members`）

**概要**: 案件に所属するスタッフの情報を管理します。

**スタッフ一覧**:
- アカウント番号・名前・セクション・役割・在籍期間を一覧表示
- スタッフをクリックして詳細編集

**編集できる項目**:

| 項目 | 内容 |
|------|------|
| 表示名 | 画面に表示される名前 |
| 所属セクション | 複数選択可（SV/査定/販売/MOTAなど） |
| 役割 | スタッフ / 案件管理者 |
| アカウント番号 | 番付管理・座席表に使用 |
| 稼働日数設定 | 月X日 / 週X日 / スポット / 未設定 |
| 希望シフト | 主に希望するシフトパターン |
| 連続勤務上限 | X日以上連続しないようにシフト制御 |
| 在籍期間 | 入社日・退社日 |
| 離職リスク | フラグON時に管理者向けにアラート表示 |
| シフト備考 | 仮組み時に参照するメモ |

**稼働日数「スポット」**:
- スポット設定のスタッフはシフト仮組みの対象外になります

**LINE連携状態**:
- 各スタッフのLINE連携状況（連携済み/未連携/友達追加済み/未追加）を確認

**番付タブ**:
- ASS査定・ASS販売の番付をExcelからインポート
- 休憩スロット自動割り当ての優先順位に使用

---

### 4-5. 座席表（`/seating`）

**概要**: フロアマップ上にスタッフを配置し、リアルタイムの席状況を管理します。

> 当日状況タブ③にも同じ座席表が埋め込まれています

**座席カードの情報**:
- スタッフ名・シフト名・アカウント番号
- 状態色（緑=勤務中、橙=休憩中、赤=超過、グレー=離席、濃グレー=退勤）
- 休憩スロットバッジ（①②③）

**打刻操作（座席タップ）**:
- **出勤打刻**: 出勤前のスタッフに対して出勤打刻できる
- **退勤打刻**: 早退 / 定時 / 残業の3択を選択
- **離席・着席**: 席を外す/戻る操作
- **休憩開始・終了**: 休憩の開始と終了
- **休憩設定変更**: 管理者のみ：スロット変更・休憩時間変更

**編集モード**:
- 「編集」ボタンで席替えモードに切り替え
- スタッフカードを座席にドラッグ&ドロップで配置
- 配置保存時に休憩スロットが自動割り振りされる
- 未配置スタッフは右パネルに表示

---

### 4-6. 周知管理（`/notices/manage`）

**概要**: スタッフへのお知らせを作成・送信します。

**作成フォーム**:

| 項目 | 内容 |
|------|------|
| タイトル | お知らせのタイトル |
| 本文 | お知らせの内容（文字数制限なし） |
| 宛先 | 全スタッフ / 特定のスタッフ（個人選択） |
| ピン留め | ONにすると全スタッフのお知らせ画面で常に上部表示 |
| LINE通知 | ONにすると送信と同時にLINEで通知 |
| 投稿日時 | 過去の日時でも投稿可能 |

**LINEで届く通知**:
```
【お知らせ】
宛先：全スタッフ
送信者：田中
─────────────────
[タイトル]

[本文（全文）]
─────────────────
[周知事項を見る] ← ボタン
```

**送信履歴**:
- 送信済みのお知らせはアコーディオンで一覧表示
- 送信済みのお知らせは編集・削除不可

---

### 4-7. 問合せ管理（`/inquiries/manage`）

**概要**: スタッフから届いた問い合わせに返信します。

**機能**:
- 問い合わせ一覧（未対応バッジ表示）
- 各問い合わせをクリックして内容確認・返信入力
- 返信すると該当スタッフにLINE通知（全文）が届く
- ステータス管理（受付中 / 解決済み）

---

### 4-8. LINE設定（`/line-settings`）

**概要**: LINEグループとの連携設定および通知設定を行います。

**設定タブ①: 通知設定**

各通知イベントのON/OFFを切り替えます:

| 通知 | 内容 | 送信先 |
|------|------|--------|
| 欠勤申請 | スタッフが欠勤報告した時 | 管理者グループ |
| 遅刻申請 | スタッフが遅刻報告した時 | 管理者グループ |
| お知らせ | 周知事項を送信した時 | スタッフ個人 |
| 問い合わせ / 返信 | 問い合わせの往復 | 管理者 / スタッフ |
| シフト変更 | シフトが変更された時 | 対象スタッフ |
| 勤怠補正結果 | 補正申請の審査結果 | 申請したスタッフ |
| 翌日出勤リマインド | 翌日出勤者への前日通知 | スタッフ個人 + グループレポート |
| 希望休受付開始 | 希望休の申請期間開始時 | スタッフ |
| 欠勤経過報告リマインド | 欠勤当日17時ごろ | 欠勤スタッフ |

**翌日出勤リマインドの「今すぐ送信」ボタン**:
- 翌日出勤者への個人リマインドを即時送信
- 管理グループへのサマリーレポートも同時送信:
  ```
  【翌日（M/D）出勤リマインドレポート】
  合計 N名 / SV N名 / 査定 N名 / 販売 N名...
  ⚠️ 送信失敗 N名
  ----------
  【SV】
  ・ASS 03 田中純也
  【査定】
  ・ASS 11 鈴木大地  ※前日欠勤
  ・ASS 12 佐藤花子  ✗未送信（LINE未登録）
  ```

**設定タブ②: 連携管理**

- LINEグループIDの設定・変更
- スタッフ別のLINE連携状態・友達追加状況の確認
- 「未確認者に再送（N名）」ボタン: テスト通知をまだ確認していないスタッフのみに再送

---

### 4-9. 希望休承認（`/holidays/manage`）

**概要**: スタッフからの希望休申請（`holiday_requests` — 承認制）を管理します。

> `shift_off_requests`（優先度付き希望休）とは別の、正式な希望休申請です

**機能**:
- 申請一覧・月別フィルター
- 承認 / 却下操作

---

### 4-10. タスク（`/tasks`）

**概要**: LINEグループから抽出されたタスクを管理します。

**機能**:
- タスク一覧（担当者・期限・ステータス）
- LINEグループメッセージからの自動抽出（`task_extraction_groups` 設定が必要）
- 担当者割り当て（割り当て時にLINE通知）

---

### 4-11. ランキング（`/rankings`）

**概要**: 番付データを確認します。

---

### 4-12. 案件設定（`/admin/[projectId]/settings`）

**概要**: 案件に関するすべての設定を行います。

**設定タブ一覧**:

#### 基本設定
- 案件名の変更
- 有効/無効の切り替え（無効にするとスタッフが選択できなくなる）
- 出発報告機能の有効化/無効化

#### シフトパターン設定
- シフト名・セクション・開始時刻・終了時刻・必要人数の登録・編集
- ソート順の管理

#### 座席レイアウト設定
- フロアマップ上の座席・壁をドラッグで配置
- 座席タイプ（通常 / フリー席 / 無効）の設定

#### 休憩設定
- 休憩スロット①②③の時間帯・対象割合の設定
- デフォルト: ①12:00-13:00（早番20%）/ ②13:15-14:15（全体40%）/ ③14:30-15:30（遅番40%）

#### 希望休ルール設定
| ルール | 内容 |
|--------|------|
| 申請受付開始日 | 毎月何日から申請可能か |
| 申請締め切り日 | 毎月何日で締め切るか |
| 1人あたり月上限 | 1人が月に申請できる最大日数 |
| 土日申請上限 | 土日に申請できる最大日数 |
| 1日あたり申請上限 | 1日に申請できる最大人数 |
| 連続申請上限 | 連続して申請できる最大日数 |

#### スプレッドシート連携
- Google スプレッドシートとの連携設定
- シフトインポート元スプシのURL設定

---

## 5. 運営者メニュー

> `global_role = "executive"` のみアクセス可能

### 5-1. 案件管理（`/admin`）

**概要**: 全案件を横断的に管理します。

**機能**:
- 全案件の一覧（アクティブ / アーカイブ）
- 新規案件の作成
- 各案件の設定ページへのリンク
- 案件のアーカイブ（非表示化）

---

### 5-2. 運用者管理（`/admin/operators`）

**概要**: 運用者（executive）アカウントを管理します。

**機能**:
- 運用者一覧の表示
- 新規運用者アカウントの追加（Supabase Auth にユーザー作成）
- 有効/無効の切り替え
- 初回ログイン時のパスワード変更フラグ管理

---

### 5-3. スタッフ一覧（`/admin/staffs`）

**概要**: 全スタッフアカウントを横断的に管理します（全案件共通）。

**機能**:
- 全スタッフの一覧（全案件横断）
- 新規スタッフアカウントの作成
  - 社員ID（例: S001）を入力 → `s001@raq.internal` で自動作成
  - 初期パスワードを設定
- 表示名・会社名の編集
- グローバルロールの変更（staff / admin / executive）
- LINE連携状態の確認

---

## 6. 打刻端末（タブレット）

**URL**: `https://raq-portal-app.vercel.app/punch/[案件ID]`

**概要**: 店舗・現場に設置するタブレット専用の打刻ページです。認証不要で使用できます。

---

### 出勤打刻の流れ

1. スタッフ名をタップ（「出勤」ボタンが表示）
2. 「出勤する」をタップ

**打刻時刻のルール**:

| 状況 | 記録される時刻 | note記録 |
|------|-------------|---------|
| シフト開始時刻**より前**に打刻 | シフト開始時刻に補正 | `出勤打刻: HH:MM` |
| シフト開始時刻**より後**に打刻（遅刻） | 実打刻時刻を**15分切り上げ** | `出勤打刻: HH:MM` |

> 例: シフト9:00開始、8:45に打刻 → 9:00として記録  
> 例: シフト9:00開始、9:07に打刻 → 9:15として記録（遅刻）

遅刻の場合は承認SVの名前入力画面に進みます。

---

### 退勤打刻の流れ

1. スタッフ名をタップ（「退勤」ボタンが表示）
2. 退勤の種別を選択

**退勤の3択**:

| 選択 | 記録時刻 | 承認SV | note記録 |
|------|---------|--------|---------|
| **早退** | 実打刻時刻を**15分切り下げ** | 必須入力 | `退勤打刻: HH:MM  早退承認者: ○○` |
| **定時退勤** | シフト終了時刻 | 不要 | `退勤打刻: HH:MM` |
| **残業** | 実打刻時刻**そのまま** | 必須入力 | `退勤打刻: HH:MM  残業承認者: ○○` |

> 例: シフト18:00終了、17:47に打刻 → 「早退」選択 → 17:45として記録  
> 例: シフト18:00終了、18:02に打刻 → 「定時退勤」選択 → 18:00として記録  
> 例: シフト18:00終了、19:30に打刻 → 「残業」選択 → 19:30として記録

---

### 休憩操作

スタッフ名の「離席」ボタン → 離席/着席のトグル  
スタッフ名の「休憩」ボタン → 休憩開始/終了のトグル

---

## 7. 共通仕様・ルール

### 打刻記録の仕様

- 全打刻は `punch_logs` テーブルに記録
- 打刻種別: `clock_in`（出勤）/ `clock_out`（退勤）/ `seat_leave`（離席）/ `seat_return`（着席）/ `break_start`（休憩開始）/ `break_end`（休憩終了）
- **実打刻時刻は必ず `note` 欄に記録される**（勤怠実績の備考欄で確認可能）

### 打刻状態と勤怠ステータスの分離

- **勤怠ステータス**（出勤簿表示）: 管理者が手動変更可能
- **打刻状態**（実際の打刻有無）: スタッフ本人の実打刻のみ
- 管理者が「勤務中」に手動変更しても、スタッフ本人は打刻画面でいつでも通常打刻できます

### Cron自動処理（毎日定時実行）

| 時刻 | 処理内容 |
|------|---------|
| 17:00 | 当日欠勤スタッフへの「経過報告」LINEボタン通知 |
| 20:00 | 翌日出勤スタッフへのリマインド通知 + グループレポート |
| 09:00（設定日のみ） | 希望休申請受付開始の通知 |

### タイムゾーン

- 全データは **日本標準時（JST / Asia/Tokyo）** で処理・表示
- サーバー内部はUTCで保存しているが、表示は常にJSTに変換

### LINE通知について

- スタッフはLINE連携 + 公式アカウント友達追加が必要（両方必須）
- LINE未登録のスタッフには通知が届きません（送信エラー = 失敗カウント）
- 案件ごとに通知のON/OFFを設定できます

---

*本書は Raq Works の現行機能を記載したものです。機能追加・変更に応じて更新されます。*
